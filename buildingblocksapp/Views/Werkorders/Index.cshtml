@model IEnumerable<buildingblocksapp.Models.Werkorder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Motortype)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LeverPeriode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Klantorder)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Motortype)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LeverPeriode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Klantorder.Naam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WerkorderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WerkorderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WerkorderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
